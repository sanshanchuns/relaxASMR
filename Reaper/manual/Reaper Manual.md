首选项 【Ctrl + P】
    - 音频 - 设备，一般默认选 WaveOut
    - 如果有自己的音频设备【独立声卡】，选 ASIO[音频流输入输出，高性能，低延迟]
    - 如果是windows自带的音频，选 WASAPI
    - 常规 - 路径，设置保存路径，Reaper Saves, Reaper Media, ReaPeaks
    - 项目 - 项目设置，勾选 新项目时提示保存
    - 项目 - 片段循环默认值

新建项目 
    - 项目设置 - 媒体，设置保存媒体文件的路径， Audio Files
    - 勾选为项目创建子目录

工作区 - 左轨右编，编排区可以放任何片段：音频、视频、MIDI、图片

工具栏
    - 网格
    - 吸附【默认4像素，可以更大，这样更远的距离就会自动吸附】，可以设置吸附到网格、片段、选区，光标

编排区
    - 速度、网格、吸附
        - 右击播放速率，更改主播放速率时保留音高 pitch（加减速但不变音）
        - BPM【Tempo】调节片段速度，如果不想片段速度被破坏，项目设置 - 时间基准 - 时间，这样变更 BPM 之后，片段速度还是保持不变
        - 插入速度/拍号标记，设置标记之间使用不同的速度，支持速度渐变【标记之间的线就是包络线，也就是速度线】
    - 拖拽
        - 右击拖动 - 选中多个片段
        - 片段边缘拖动 - 循环延长音频【片段右击，片段设置，默认循环片段源 】
        - 片段角边缘拖动 - FadeIn FadeOut
        - w，光标自动到头部【✅】
        - 边缘拉伸，Alt + 拖动， 可以改变 pitch，也可以通过 片段属性 选项来改变
        - 片段拉伸, Alt + 拖动，可以改变呈现的部分【选择呈现哪一部分】 
        - 拉伸标记，Ctrl + Alt + 左击
    - 缩放
        - 点击片段，滚轮会横向放大光标所在位置的细节
        - Ctrl + 滚轮，升高鼠标所在的轨道【外观 - 缩放/滚动/偏移 - 纵向缩放中心 - 鼠标下的轨道；横向缩放中心：鼠标光标】
        - Ctrl + win + 右击拖动，放大局部
        - Ctrl + PageDown, 当前轨道总览
        - ~, 所有轨道最小化
    - 选区
        - 左击滑动标尺区域，框选区域，点击重复播放可播放框选区域
        - 左击片段1 + shift + 左击片段3，那么会选中 片段1、2、3；水平、垂直都有一样的效果；不想连续选中的话，使用 Ctrl,就可以单个选中
        - 淡入淡出多个片段，这些片段末尾必须对齐
    - 循环
        - 首选项 - 项目 - 片段循环默认值 - 设置哪些会循环【导入的片段、录制的片段、合并的片段】
        - 片段右击 - 片段设置 - 循环片段源
    - 复制
        - 方式1：分割1、分割2，复制1-2，Ctrl + V，拖动
        - 方式2：选中区域[Esc取消选中]，Ctrl + Shift + C，Ctrl + V，拖动
    - 分割
        - 光标定位，s 分割，选中被分割的两段，heal 合并
        - 分割之后，针对被分割的片段，上下移动可单独调节音量，可配合 Shift[相对值]，也可以通过旋钮调节音量
    - FX
        - 可以针对部分片段添加 FX[比如JS特效], 添加之后的片段左上角会出现 FX 图标
    - G，分组【Folder 父轨可加 FX，子轨音频先过父轨再出 Master — 适合统一削高频】
    - M, 标记
    - V, 音量
    - Shift + R，区域，类似标记
    - Ctrl + Alt + R，渲染，可以按区域分别渲染，输出多个文件
    - 操作 - 改快捷键

ReaScript（自动化脚本）
    - Reaper **不支持 JavaScript** 做 ReaScript；可用 **Lua**（推荐）、**Python**、**EEL**
    - 菜单 **Actions → Show action list → ReaScript: New/Load** 加载 `.lua` 脚本
    - **ForestRainNight 工程**（自包含）：
        - `Reaper/ForestRainNight/` — `.rpp` + `scripts/`（含 `asmr_config.lua`）
        - 主脚本：`ForestRainNight/scripts/asmr_setup_project.lua`
        - 说明：`Reaper/ForestRainNight/README.md`
    - 单轨手动：`ForestRainNight/scripts/random_scatter_items.lua`
    - **JS 特效（JSFX）** 是音频效果，不能排 item；排布用 ReaScript Lua
    - 可选：**ReaPack**、**SWS Extension**


轨道区
    - 从模板新建【高级功能】
    - 空不出右击，显示主轨道
    - 路由、FX【比如EQ】、音量、相位【小鼓】、自动化曲线（包络），左右声道比例
    - 多选轨道【临时组】，同时 Mute, 同时 Solo
    - 右击轨道，设置轨道随机颜色
    - 选项 - 主题 - 主题调整器 - 自定义颜色 - 随机切换
    - FX
        - 多个常用的效果可以保存成模板，之后从模板加载进某个轨道
        - 点击并拖动 轨道1 的效果 到 轨道2，即可完成 效果 复制
        - 点击效果1 并拖动到 轨道2，轨道1、2都有效果1
        - Alt + 点击效果1 并拖动到 轨道2，那么会移动 效果1 到 轨道2，轨道1 将没有效果1
        - Alt + 点击 FX，清除当前轨道上的效果
        - 主轨道 上添加 FX, ReaLimit，Loudness

录音
    - 延迟 Latency，WASAPI 30ms，WaveOut 170ms，ASIO 0ms【硬件监控】
    - 手动 punch（覆盖录音）
    - 时间选择自动 punch
    - 片段选择自动 punch
    - 选项 - 录段行为 - 显示空录段（关闭之后就只会显示有效的录段）
    - 录段（片段）右击 - 录段 - 裁切至当前录段 / 锁定当前录段
    - 选项 - 重叠录音行为 - 修剪现有片段（磁带模式）/ 分层添加媒体片段（两层添加，太混乱了）